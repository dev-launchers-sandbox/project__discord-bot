/* This file will store the constructor for the users object
   Functions that add users and edit their inventory/currency will also be stored here
*/
/*const usersArray = [
  {
    username: "Guillermo",
    id: "647869800994832450"
  } //hello?
];
module.exports = {
  newUser: function(ID, member) {
    const user = usersArray.find(user => user.id === member.user.id);
    if (user) {
      console.log(user.username);
    }

    this.userID = ID;
    this.userCurrency = 0;
    this.userInventory = [];
  },
  getCurrency: function() {
    return this.userCurrency;
  },
  changeCurrency: function(amount) {
    this.userCurrency = this.userCurrency + amount;
  },
  getInventory: function() {
    return this.userInventory;
  },
  addItem: function(item) {
    this.userInventory.push(item);
  }
};
*/
